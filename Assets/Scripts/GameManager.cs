using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class GameManager : MonoBehaviour
{
    public float timeFlow = 1; //This is the rate of time, affecting everything in the game (other then the player). Default to 1

    public Slider timeResourceBar;
    public GameObject timeSlowOverlay;

    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
